<% if current_user.favorites.exists?(product: @product) %>
  <%= button_to 'Unfavorite', product_favorite_path(@product), method: :delete, class: 'btn btn-secondary' %>
<% else %>
  <%= button_to 'Favorite', product_favorites_path(@product), class: 'btn btn-secondary' %>
<% end %>